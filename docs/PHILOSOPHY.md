# Philosophy

Do one thing exceptionally well.

Not an ORM.
Not a validator.
Not a query builder.
Not a SQL generator.

Only compile metadata into SQLAlchemy expressions.

If a Pydantic feature cannot be translated deterministically, SQLRules
refuses rather than guessing.
